
fun outer(x) {
  fun middle(y) {
    var t = x + y;
    fun inner(z) {
      return x + y + z;
    }
    print "Middle: ";
    print t;

    return inner;
  }
  return middle;
}

print outer(1)(2)(3);
