fun outer(x) {
  fun middle(y) {
    // var middleX = x;
    fun inner(z) {
      print x + y + z;
    }
    return inner;
  }
  return middle;
}
var closure = outer(1)(2);
closure(3); // Should print 6

var c1 = outer(1);
var c2 = c1(2);
c2(3); // Should also print 6
